# O app já vem compilado neste repositório — o Netlify não constrói nada,
# apenas publica os arquivos como estão. Sem npm, sem build, sem falha de build.
[build]
  publish = "."
  functions = "netlify/functions"

[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# SPA: qualquer rota cai no index (funções têm precedência)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
